spring:
  profiles:
    default: local

  datasource:
    url: jdbc:h2:mem:~/h2
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none

---
spring:
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        show_sql: true

    # (boot 2.5~) Hibernate ??? ? data.sql ??
    # ?? ?? Hibernate ??? ?? ??, ?? ??
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true

---
spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        show_sql: true

  sql:
    init:
      mode: never
